# 調査レポート: PMXテクスチャ白化問題

## 結論
PMXモデルのテクスチャが白っぽく表示される問題の主原因は**テクスチャディレクトリの重複**です。

## 判明した事実
1. テクスチャが `mmd/texture/` と `texture/` の両方に存在
2. システムは `mmd/texture/` を優先読み込み
3. 作成したPNGフォールバックは別ディレクトリにあり機能せず

## 推奨対応
1. ディレクトリ構造を一本化
2. Unity Color Space設定を確認
3. 必要に応じてTGA→PNG一括変換

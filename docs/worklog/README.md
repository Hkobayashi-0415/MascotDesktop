# Worklog

- 変更ごとに `docs/worklog/` へログを追加する（最低限の根拠・再現性をリポジトリ内に残す）。
- 1エントリ=1タスク（推奨）。ファイル名は `YYYY-MM-DD_<short-title>.md`。
- 書式は `docs/worklog/_template.md` をベースにし、`Used-Skills` と `External-Refs` を必ず埋める。

